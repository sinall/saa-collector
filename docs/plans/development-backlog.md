# Collector 开发 Backlog

本文档记录运行、排查和日常使用中发现的待修问题。这里的问题不一定属于同一个设计主题，进入实现前需要再拆分优先级和提交边界。

## 待修问题

暂无。

## 已修复问题

### 长任务详情页轮询时持续显示整页加载蒙层

状态：已修复。

现象：

- 从“采集日程”点击“执行”创建采集计划并跳转到采集计划详情页后，如果计划进入长时间 `RUNNING` 状态，详情页会一直显示转圈加载蒙层。
- 财务报表等采集任务可能持续数十小时，整页蒙层会挡住详情和任务状态，实际意义不大。

修复：

- 采集计划详情页只在首次加载时显示整页加载蒙层。
- 长任务 `RUNNING` 期间继续后台轮询刷新状态，但不再覆盖页面内容。

### 缺少真实分页

状态：已修复。

现象：

- `/admin/collector/collect-plans` 当前只加载有限数量的最新计划后在前端按 tab 过滤。
- 如果定时触发的财务报表采集计划不在这批数据内，`定时触发` tab 可能只显示较新的 `Tick数据采集...`，看不到较早的 `财务报表采集(5月)...`。
- 页面没有分页控件，用户无法继续翻找历史采集计划。

修复：

- 后端 `CollectPlan` 列表 API 支持 `source`、`status`、`trigger_type` 等查询参数。
- 前端 tab 切换时发起服务端过滤请求，而不是只过滤当前已加载的前 100 条数据。
- 前端展示分页控件，使用后端返回的 `count` 驱动翻页。

### 采集日程执行后的计划名称短暂错误

状态：已修复。

现象：

- 在“采集日程”页对 `财务报表采集(5月)` 点击“执行”后，新创建的采集计划初始显示为 `Tick数据采集...`。
- 刷新页面后，该采集计划才变为正确的 `财务报表采集(5月)...`。

初步判断：

- 触发后前端可能使用了过期列表状态、默认展示值或错误的临时对象。
- 后端持久化后的数据看起来是正确的，因为刷新后显示恢复正常。

修复：

- 执行成功后以前端收到的后端 plan 数据为准更新列表。
- 修复 `CollectPlanSerializer.source_schedule_id` 误读关系字段的问题，触发接口返回完整的新 plan。
- 手动触发日程后刷新日程列表，并使用后端返回的 plan id 跳转详情，避免默认名称污染新计划展示。

### worker 重启后执行中计划未自动释放

状态：已修复。

现象：

- 财务报表采集计划执行中重启 `saa-collector-worker` 后，采集计划详情页仍显示 `执行中`。
- 页面只在 `COMPLETED` 或 `FAILED` 状态下显示“重新执行”按钮，导致用户无法直接触发续跑。

修复：

- collector 队列 worker 启动前自动执行 `cleanup_interrupted_collect_tasks`。
- 清理逻辑把孤儿 `RUNNING` plan/job 标记为 `FAILED`，保留 `job.config.remaining_symbols`，重新执行时仍可续跑。
- scheduler 队列 worker 不执行该清理，避免 scheduler 重启误伤正在运行的 collector 任务。
